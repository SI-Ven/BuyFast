<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.buyfast.modules.user.repository.UserRepository">

    <resultMap id="UserResultMap" type="com.example.buyfast.modules.user.model.User">
        <id property="id" column="user_id"/>
        <result property="publicId" column="public_id"
                typeHandler="com.example.buyfast.security.config.UUIDTypeHandler"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>

        <collection property="roles" ofType="com.example.buyfast.modules.user.model.Role">
            <id property="roleId" column="role_id"/>
            <result property="name" column="role_name"/>
            <result property="description" column="role_desc"/>
            <result property="isActive" column="role_active"/>

            <collection property="permissions" ofType="com.example.buyfast.modules.user.model.Permission">
                <id property="permissionId" column="perm_id"/>
                <result property="permissionKey" column="perm_key"/>
                <result property="description" column="perm_desc"/>
            </collection>
        </collection>
    </resultMap>

    <sql id="selectUserWithRolesAndPerms">
        SELECT
            u.id          AS user_id,
            u.public_id,
            u.username,
            u.email,
            u.password,
            u.is_active,
            u.created_at,
            u.updated_at,

            r.id          AS role_id,
            r.name        AS role_name,
            r.description AS role_desc,
            r.is_active   AS role_active,

            p.permission_id  AS perm_id,
            p.permission_key AS perm_key,
            p.description    AS perm_desc

        FROM auth.users u
                 LEFT JOIN auth.user_roles ur ON u.id = ur.user_id
                 LEFT JOIN auth.roles r ON ur.role_id = r.id
                 LEFT JOIN auth.role_permissions rp ON r.id = rp.role_id
                 LEFT JOIN auth.permissions p ON rp.permission_id = p.permission_id
    </sql>

    <select id="findById" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        WHERE u.id = #{id}
    </select>

    <select id="findByPublicId" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        WHERE u.public_id = #{publicId}::uuid
    </select>

    <select id="findByUsername" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        WHERE u.username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        WHERE u.email = #{email}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        ORDER BY u.created_at DESC
    </select>

    <select id="findByIsActive" resultMap="UserResultMap">
        <include refid="selectUserWithRolesAndPerms"/>
        WHERE u.is_active = #{isActive}
        ORDER BY u.created_at DESC
    </select>

</mapper>